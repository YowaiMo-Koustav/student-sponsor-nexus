import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const SponsorResponseRateChart = () => (
  <Card>
    <CardHeader>
      <CardTitle>Sponsor Response Rate (Demo)</CardTitle>
    </CardHeader>
    <CardContent>
      <div className="text-center text-muted-foreground py-8">
        Demo data: 92% response rate
      </div>
    </CardContent>
  </Card>
);

export default SponsorResponseRateChart; 