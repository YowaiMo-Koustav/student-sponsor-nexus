import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const SponsoredEventsPortfolio = () => (
  <Card>
    <CardHeader>
      <CardTitle>Sponsored Events Portfolio (Demo)</CardTitle>
    </CardHeader>
    <CardContent>
      <div className="text-center text-muted-foreground py-8">
        Demo data: 8 events sponsored
      </div>
    </CardContent>
  </Card>
);

export default SponsoredEventsPortfolio; 